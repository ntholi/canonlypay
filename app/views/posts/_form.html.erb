<%= form_for(setup_post(post), :html => {:class => "form-horizontal"}) do |f| %>
<% if post.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(post.errors.count, "error") %>
    prohibited this post from being saved:</h2>

  <ul>
    <% post.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-group">
  <%= f.label :content, "Description", class: 'col-sm-3 control-label' %>
  <div class="col-sm-9">
    <%= f.text_area :content, class: 'form-control' %>
  </div>
</div>

<%= f.fields_for :product do |fp| %>
<div class="form-group">
  <%= fp.label :name, "Product Name", class: 'col-sm-3 control-label' %>
  <div class="col-sm-9">
    <%= fp.text_field :name, class: 'form-control' %>
  </div>
</div>

<div class="form-group">
  <%= fp.label :product_category_id, class: 'col-sm-3 control-label' %>
  <div class="col-sm-9">
    <%= fp.collection_select :product_category_id, ProductCategory.all, :id, :category, {}, {class: 'form-control'} %>
  </div>
</div>
<% end %>

<div class="form-group">
  <%= f.label :min_price, "Price: ", class: 'col-sm-3 control-label' %>
  <div class="col-sm-9">
    <div class="input-group">
      <div class="input-group-addon">R</div>
      <%= f.text_field :min_price, class: 'form-control', placeholder: "Amount" %>
    </div>
  </div>
</div>

<div class="form-group">
  <div class="col-sm-offset-3 col-sm-9">
    <div class="checkbox">
      <label>
        <%= f.check_box :payable %>
        *Pay to Post
      </label>
    </div>
  </div>
</div>

<div class="form-group">
  <div class="col-sm-offset-3 col-sm-9">
    <%= f.submit "Post", class: 'btn btn-default'  %>
  </div>
</div>
<% end %>
<p>
  *Advantages of running a paid post
  <ul>
    <li>Your post will be in our page for a long time</li>
    <li>Your post will be on top of our page</li>
    <li>Your post will appear in a more catchy way hance attract more bidders</li>
    <li>The fact that you spend to have your post on the system will give sellers, especially businesses that you really want the product or service hance they will conduct you. Meaning paid posts increases the chances of receiving bids from reliable sellers</li>
  </ul>
</p>
ur post will be on top of our page</li>
<li>Your post will appear in a more catchy way hance attract more bidders</li>
<li>The fact that you spend to have your post on the system will give sellers, especially businesses that you really want the product or service hance they will conduct you. Meaning paid posts increases the chances of receiving bids from reliable sellers</li>
</ul>
</p>
you. Meaning paid posts increases the chances of receiving bids from reliable sellers</li>
</ul>
</p>
